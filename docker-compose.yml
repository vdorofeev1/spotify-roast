services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: spotifyroast
      POSTGRES_USER: roast
      POSTGRES_PASSWORD: roast
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U roast"]
      interval: 5s
      retries: 5
    restart: unless-stopped

volumes:
  pgdata:
